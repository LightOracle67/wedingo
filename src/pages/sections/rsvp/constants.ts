/** Alergías comunes ofrecidas como chips (las claves son i18n rsvp.allergies.<a>). */
export const ALLERGIES = ["sin gluten", "sin lactosa", "alergia frutos secos", "alergia mariscos"] as const;

/** Máximo de acompañantes adultos por invitado en la UI (los niños se indican aparte con un contador). */
export const MAX_COMPANIONS = 2;

/** Máximo de niños que un invitado puede declarar como acompañantes. */
export const MAX_CHILDREN = 10;
